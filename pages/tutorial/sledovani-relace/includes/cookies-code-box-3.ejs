<div data-project="12" data-project-extends="11" class="u-mb-2">
<pre data-code="JmenoServlet.java" data-java-package data-java-package-opened data-active><code>import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class JmenoServlet extends HttpServlet {
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {
        String jmeno = req.getParameter("jmeno");

        Cookie cookie = new Cookie("jmeno", jmeno);
        res.addCookie(cookie);

        res.sendRedirect("./jmeno");
    }
}</code></pre>
</div>